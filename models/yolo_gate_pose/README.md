# YOLO Gate Pose Model

Reserved for YOLO gate pose model metadata and local weights used by the
experimental camera localization module.

Large weight files should usually stay out of git. If weights are stored
externally, document the local path, model format, input size, and keypoint order
in `model_meta.json`.

